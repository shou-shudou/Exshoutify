# Exshoutify

A music downloader project for Termux.

## Installation

To install the required dependencies, run:
```bash
pip install -r requirements.txt
```

## Usage

Run the following command to start the downloader:
```bash
python main.py
```